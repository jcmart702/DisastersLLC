import test from 'ava';

test('module should expose SAM', t => {
  // Ensure tests only pass when a specific number of assertions have 
  // been executed
  t.plan(2);

  const SAM = require('..');
  t.truthy(SAM);
  t.is(typeof SAM, 'function');
});